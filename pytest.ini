[pytest]
pythonpath = .
# The BrowserStack SDK plugin intercepts driver setup, so it stays off for local runs.
# `browserstack-sdk pytest` loads it regardless of this setting.
addopts = -p no:browserstack_sdk
markers =
    testrail: mark test to be executed and reported to TestRail
