[project]
name = "pytest-browserstack-web"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "browserstack-sdk>=1.37.0",
    "pytest-html>=4.2.0",
    "pytest-playwright>=0.7.2",
    "requests>=2.32.5",
    "ruff>=0.15.2",
]
